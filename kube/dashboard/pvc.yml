apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashboard
  namespace: dashboard
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
